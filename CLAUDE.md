# CivilChain - プロジェクト方針

## プロジェクト概要

Go言語で実装するブロックチェーンプロジェクト。
金銭の記録ではなく、**法律・公約などの公的記録**を改ざん不可能な形で残すことを目的とする。

## 開発方針

### ステップ1: ミニビットコイン実装
シンプルなビットコイン的ブロックチェーンを一つずつ実装しながら、ブロックチェーンの仕組みを理解する。

### ステップ2: CivilChainへ改造
ビットコインの実装を土台に、政治記録用のデータ構造へ置き換えていく。
何をどう変えるかは実装を進めながら自分で考える。

### ステップ3: 機能拡張（将来）
ステップ2が完成してから考える。

## Claude との作業ルール

- Bashコマンドの実行はユーザー自身が行う。Claudeはコマンドを提示するだけ。
- ファイルの編集についてはClaudeが行う。
- テストが PASS したタイミングなど、区切りのよい場面では Git コミットを提案する。
- CLAUDE.md を修正したタイミングでも Git コミットを提案する。
- コミット前に人間が差分を1行ずつ確認する。AIが書いたコードをレビューなしでコミット・プッシュしない。

## 開発スタイル

- **TDD（テスト駆動開発）**で進める
- 順序: インターフェース（シグネチャ）→ **必要なテストの列挙（ユーザーに提示・確認）** → テスト実装 → 実装
- コードは1行ずつ段階的に追加する
- テストはAAA（Arrange・Act・Assert）パターンで書く（コードに `// Arrange` などのコメントは書かない）
- テストが通ることを確認してから次のステップへ進む
- **テストは1つずつ提示・実装・確認する。複数まとめて書かない。**

### newBlock() のテスト ✅ 完了
- [x] Index — prev.Index + 1 になっていること
- [x] Timestamp — 空でないこと（現在時刻が自動セットされる）
- [x] Data — 渡したデータが入っていること
- [x] PrevHash — 前のブロックのハッシュが設定されること
- [x] Hash — ハッシュが計算されて設定されること

### Blockchain のテスト ✅ 完了
- [x] ジェネシスブロックが1つある
- [x] ジェネシスブロックのIndexが0
- [x] addBlock後にブロック数が増える
- [x] 追加したブロックのPrevHashが前のブロックのHashと一致する
- [x] 追加したブロックにデータが正しく入る

### isTampered() のテスト ✅ 完了
- [x] 正常なチェーンは `false` を返す
- [x] Data が改ざんされた場合は `true` を返す
- [x] Data と Hash が両方改ざんされた場合は `true` を返す（PrevHash チェックが必要）

### 次のステップ（ステップ2）

#### PoW（Proof of Work）実装 ✅ 完了
- [x] `Block` に `Nonce` フィールドを追加
- [x] `hash()` に `Nonce` を含めるよう変更
- [x] `mine(b Block, difficulty int) Block` を実装（Nonce を増やしてハッシュ条件を満たす）
- [x] `mine()` のテストを書く
- [x] `newBlock()` をマイニング対応に更新する
- [x] 難易度は固定値（`difficulty=4`）

#### HTTP ノード実装 ✅ 完了
- [x] `GET /chain` — チェーン全体を返す
- [x] `POST /block` — ブロックを追加する
- [x] 標準ライブラリ（`net/http`）のみで実装
- [x] ポートをコマンドライン引数（`-port`）で指定できるようにする

#### saveChain() のテスト
- [x] ファイルが作成される
- [x] 有効なJSONが書き込まれる
- [x] ブロック数が一致する
- [x] ブロックのデータ（`Data` フィールド）が正しく保存される
- [x] 書き込み不可なパスを渡すとエラーが返る

#### チェーンファイルのポート番号別分離 ✅ 完了
- [x] 保存先を `data/chain_<port>.json` 形式にする（例: `chain_8080.json`）
- [x] ポート番号を `saveChain()` に渡すか、ファイルパスを起動時に決定する

#### 最長チェーンルール
- [ ] 他ノードの `/chain` を取得して比較
- [ ] 長い方を正とする

## 設計原則

### SOLID原則

| 頭文字 | 原則名 | 概要 |
|--------|--------|------|
| **S** | Single Responsibility Principle（単一責任の原則） | クラス・関数は1つの責務だけを持つ |
| **O** | Open/Closed Principle（開放閉鎖の原則） | 拡張に対して開いており、修正に対して閉じている |
| **L** | Liskov Substitution Principle（リスコフの置換原則） | 派生型は基底型と置き換え可能でなければならない |
| **I** | Interface Segregation Principle（インターフェース分離の原則） | 使わないメソッドへの依存を強制してはならない |
| **D** | Dependency Inversion Principle（依存性逆転の原則） | 上位モジュールは下位モジュールに依存してはならない、抽象に依存すべき |

## 技術スタック

- 言語: Go 1.22+
- 外部依存: 最小限（標準ライブラリ中心）
- データ保存: JSONファイル

## 学習目標

- ブロックチェーンの仕組みをコードで理解する
- Go言語の基本（構造体・インターフェース・パッケージ）を習得する
- シビックテック（市民技術）としての応用を体験する

## オープンソース方針

### リポジトリ
公開済み: https://github.com/Raiden-2200771/civil-chain

### ライセンス
**MIT ライセンス**を採用する。
- 誰でも自由に使える・改造できる・商用利用も可
- 「政治記録を誰でも自由に使えるインフラにしたい」という思想と一致
- Go製OSSのスタンダード
